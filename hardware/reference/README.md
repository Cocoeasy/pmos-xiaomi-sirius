# Reference only

GPL-2 Android device trees copied from
[LineageOS/android_kernel_xiaomi_sdm710](https://github.com/LineageOS/android_kernel_xiaomi_sdm710)
(`lineage-20`). They describe *a* Mi 8 SE, not necessarily the live DT on
the unit on the desk.

Do not flash these. Do not treat them as the mainline `sdm710-xiaomi-sirius.dts`.
Live dumps go in `../dump/` (gitignored).
